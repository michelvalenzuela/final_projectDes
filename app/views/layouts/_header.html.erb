<nav class="navbar navbar-expand-lg navbar-light bg-gray">
  <div class="container">
    <p class="h3 navbar-brand"><%= link_to "Lettdit", root_path, class: 'tittle'%></p>
   
        <nav class="bg-gray mr-2 d-none d-sm-none d-md-block">
          <form method="GET" action="/search" class="form-inline">
              <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search" name="query">
              <button class="btn btn-light my-2 my-sm-0" type="submit">Search</button>
          </form>
        </nav>
        <% if account_signed_in? %>
        
        <div class="dropdown show">
        <div class="karma float-left mt-2 mr-2 testlet" >
          <%= current_account.karma %>
          <span class="fa fa-star text-warning"></span>
        </div>
          <a class="btn btn-white dropdown-toggle" style="color:white;" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= current_account.username %>
          </a>

          <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
            <%= link_to "Profile", profile_path(current_account.username), class: "dropdown-item" %>
            <%= link_to "Settings", edit_account_registration_path, class: "dropdown-item" %>
            <%= link_to "Sign Out", destroy_account_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </div>
        
        <% else %>
        <form class="form my-2 my-lg-0">
          <%= link_to "Sign Up", new_account_registration_path, class: "btn btn-light " %>
        <% end %>
        </form>
      </div>
      
    
  </div>
</nav>
